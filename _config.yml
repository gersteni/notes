title: idoh.com
markdown: kramdown
exclude:
  - CLAUDE.md
  - doc-suggestions.md
defaults:
  - scope:
      path: ""
    values:
      layout: page
  - scope:
      path: "index.md"
    values:
      layout: home
